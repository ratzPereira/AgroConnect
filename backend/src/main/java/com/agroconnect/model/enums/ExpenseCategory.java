package com.agroconnect.model.enums;

/**
 * Operating-expense category for a machine. Used to break down operating
 * cost in the analytics view (fuel vs. parts vs. fixed costs).
 */
public enum ExpenseCategory {
    FUEL,
    PARTS,
    INSURANCE,
    TAX,
    OTHER
}
